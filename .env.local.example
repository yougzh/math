# 复制成 .env.local 即可生效（Next.js 只读取 .env.local / .env）
#
# 没有后端时，用它让 UI 先跑起来（mock 数据严格按 docs/api-contract.md 的形状构造）：
# NEXT_PUBLIC_USE_MOCK=1

# 后端基址，默认就是本机 8000 端口
# NEXT_PUBLIC_API_BASE=http://127.0.0.1:8000

# MVP 不做账号体系，缺省孩子 id
# NEXT_PUBLIC_CHILD_ID=1
