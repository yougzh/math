# 复制成 .env.local 即可生效（Next.js 只读取 .env.local / .env）
#
# S5 起前端与 API 同源（/api/v1/*，Next Route Handler），
# 不再需要 NEXT_PUBLIC_API_BASE / USE_MOCK / CHILD_ID。
# 数据库连接见 .env 的 MATH_DB_URL（本地嵌入式 PG：npm run db:init）。
